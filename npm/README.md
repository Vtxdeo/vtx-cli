# @vtxdeo/cli

Install the VTX CLI with npm and forward all commands to the bundled Rust binary.

## Install

```sh
npm install -g @vtxdeo/cli
```

## Usage

```sh
vtx --help
```

## Platform packages

The main package installs a platform-specific binary via optional dependencies.
